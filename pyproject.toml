[project]
name = "asteroids-game"
version = "0.1.0"
description = "Pygame implementation of the classic Asteroids game"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pygame==2.6.1",
]
